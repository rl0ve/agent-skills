interface:
  display_name: "Design Router"
  short_description: "Route UI work to a focused skill chain"
  default_prompt: "Use $design-router to classify this UI task and select the smallest compatible skill chain."
